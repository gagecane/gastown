<!--
This file is the auto-test-pr MR-banner template for THIS rig.

The polecat reads it at `gt done` time and substitutes the
double-curly placeholders below with values from the cycle's
dispatch-bead envelope and gate-run results. The fully-rendered banner
becomes the body of the MR bead and is the reviewer's audit receipt.

Refinery's MR-bead post-check verifies that the banner is present and
that its `Quality gates` section is consistent with the dispatch bead's
recorded gate outcomes (security-leg's defense against polecat
self-attestation). If you remove or reorder the load-bearing sections
below, the post-check will fail.

Load-bearing sections (do NOT remove or rename):
  - "🤖 Auto-generated by gt auto-test-pr (v1)" header line
  - "Target:" line
  - "Why this file:" line
  - "Origin bead:" line
  - "Conventions:" line
  - "What's covered (delta):" block
  - "Quality gates passed:" block (must list all 7 gates 4a-g)
  - "To approve for merge:" line
  - "To pause this rig:" line
  - "To turn it off:" line

Editable sections (rig-specific):
  - The "Reviewer notes" tail block at the end of the file is the only
    free-form section the polecat may extend. Any rig-specific banner
    additions go there.

Placeholders:
  {{target_path}}             — relative path to the source file under test
  {{target_lines}}            — comma-separated uncovered line numbers covered
                                this cycle (e.g., "47, 92, 158")
  {{churn_commit_count}}      — int, 30-day commit count on target file
  {{coverage_before_pct}}     — float (0-100), branch coverage before
  {{coverage_after_pct}}      — float (0-100), branch coverage after
  {{origin_bead}}             — dispatch bead id (gu-...)
  {{conventions_path}}        — usually .gt/auto-test-pr/conventions.md
  {{covered_branches[]}}      — list of {file, line, kind, reason}; rendered as
                                bullet lines under "What's covered (delta)"
  {{coverage_delta_pct_signed}} — signed string ("+16.0%", "-2.5%")
  {{flakiness_runs_passed}}   — int, e.g. 10 (out of 10)
  {{rig}}                     — "gastown_upstream" for this rig
  {{phase1_revise_cli}}       — "gt auto-test-pr revise --mr=<this-mr-bead>"
                                — included only when revision-routing
                                feature flag is OFF (Phase 1)
-->

🤖 Auto-generated by gt auto-test-pr (v1)
─────────────────────────────────────────
Target:        {{target_path}} (lines {{target_lines}})
Why this file: high churn ({{churn_commit_count}} commits in last 30d)
               × low branch coverage ({{coverage_before_pct}}% →
               {{coverage_after_pct}}% with this MR)
Origin bead:   {{origin_bead}}
Conventions:   {{conventions_path}} (read first)

What's covered (delta):
{{#each covered_branches}}
  + {{this.file}}:{{this.line}}   {{this.reason}}
{{/each}}

Quality gates passed:
  ✓ 4a coverage delta ({{coverage_delta_pct_signed}})
  ✓ 4b synthetic-mutant sanity (≤5 mutants/test, AST-aware,
       in os.MkdirTemp)
  ✓ 4c flakiness ({{flakiness_runs_passed}}/10 reruns green)
  ✓ 4d tautology linter (literal-vs-literal, NotNil-only,
       no-input-derived, zero-assertion all clear)
  ✓ 4e gitleaks (no secrets in diff)
  ✓ 4f output allow-list (test-files-only; func Test*(t *testing.T)
       form on every new test)
  ✓ 4g size budget (files & added test LOC within envelope)

How to act on this MR
─────────────────────
To approve for merge: bd update <this-mr-bead> --add-label approved-by:$USER
                      (Refinery refuses to merge auto-test-pr MRs
                      without this label when
                      auto_test_pr.require_review_approval=true; D15)

To request a revision (Phase 1 manual fallback, D17):
                      {{phase1_revise_cli}}
                      (or leave a review comment on the affected
                      lines and re-run the CLI with --comment-id)

To pause this rig:    gt auto-test-pr pause --rig={{rig}} --duration=24h
                      (or paste this in any comment on this MR:
                      `gt auto-test-pr: pause-rig-7d`)

To turn it off:       gt auto-test-pr disable --rig={{rig}}

Design doc:           .designs/auto-test-pr/synthesis.md
SEV-1 runbook:        .gt/auto-test-pr/sev1-runbook.md

<!-- Reviewer notes (rig-editable, optional) -->

Reviewer notes (gastown_upstream)
─────────────────────────────────
- Auto-test-pr writes Go unit tests only. If you see anything other
  than a `*_test.go` file in the diff, REJECT — gate 4f should have
  caught it; an escape indicates a bug worth filing.
- Every new `func Test*` MUST carry the single-line provenance marker
  `// gt:auto-test-pr origin=... covers=...` immediately above its
  declaration (D8). Missing marker → request a revision; do not
  approve.
- Tests are expected to follow this rig's conventions:
  table-driven for ≥3 cases, no `time.Sleep`, no new test-framework
  deps, fixtures under `internal/<pkg>/testdata/`. See the rig's
  conventions sheet for the full list.
