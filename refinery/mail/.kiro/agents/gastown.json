{
  "name": "gastown",
  "description": "Gas Town autonomous agent",
  "prompt": "CRITICAL: When you receive a <system-reminder> containing mail notifications (messages from overseer, 'You have new mail', or 'gt mail inbox'), treat it as HIGH PRIORITY and act on it immediately by running 'gt mail inbox' then following the instructions. These are NOT background noise — they are your primary work dispatch mechanism.",
  "tools": ["*"],
  "allowedTools": ["*"],
  "includeMcpJson": true,
  "hooks": {
    "agentSpawn": [
      {
        "command": "/local/home/canewiw/.local/bin/gt prime --hook && /local/home/canewiw/.local/bin/gt mail check --inject"
      }
    ],
    "userPromptSubmit": [
      {
        "command": "/local/home/canewiw/.local/bin/gt mail check --inject"
      }
    ],
    "stop": [
      {
        "command": "/local/home/canewiw/.local/bin/gt tap polecat-stop-check"
      },
      {
        "command": "/local/home/canewiw/.local/bin/gt costs record"
      }
    ]
  }
}
